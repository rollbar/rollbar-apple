#ifndef RollbarInternalLogging_h
#define RollbarInternalLogging_h

@import Foundation;

// This is for internal usage only
void RBCLog(NSString *format, ...);

// This is for internal usage only
void RBCErr(NSString *format, ...);

#endif /* RollbarInternalLogging_h */
